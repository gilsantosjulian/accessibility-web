// Local server URL (When the ontology is served locally)
export const LOCAL_URL = `http://localhost:3000/ontology`;

// Remote server URL (When the ontology is served in web server)
export const REMOTE_URL = `http://www.nextwo.net/html-ontology.xml`;